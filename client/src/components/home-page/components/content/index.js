import Carousel from "../carousel";

const Content = (props) => {
	return (
		<div className="p-grid">
			<div className="p-col-12">
				<Carousel />
			</div>
		</div>
	);
};

export default Content;
