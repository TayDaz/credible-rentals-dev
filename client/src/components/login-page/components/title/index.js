import "./styles.scss";

const Title = (props) => {
	return (
		<div className="p-col-12 p-text-center">
			<span className="login__heading--title">Login Page</span>
		</div>
	);
};

export default Title;
