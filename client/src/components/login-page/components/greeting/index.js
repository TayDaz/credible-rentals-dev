import "./styles.scss";

const Greeting = (props) => {
	return (
		<div className="p-col-12 p-text-center">
			<span className="login__heading--greeting">
				Welcome to the biggest rental app
			</span>
		</div>
	);
};

export default Greeting;
